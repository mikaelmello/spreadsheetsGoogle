// Mixin for all files
mixin insertLink(URL, text)
	a(href=URL, target='_blank')=text

// mixin for the page "Quem Somos"
mixin insertDeveloper(developer)
	.col-lg-4.text-center
		img.p-2.rounded-circle(src=developer.img, alt=developer.name, width="180", height="180")
		h3= developer.name
		p #{developer.text}
			br
			| Possui um portifólio no 
			+insertLink(developer.github, "GitHub")
			| .
		if (developer.cv)
			p
				a.btn.btn-secondary(href=developer.cv, role="button", target='_blank') Conheça mais »

// mixin for the page "Espaço Exploratório"
mixin digitalMedia
	label(for="digitalMedia") Mídia digital:
		select#digitalMedia.custom-select.d-block.w-100(required="")
			option(value="") Escolha...
			each media in medias
				option(value = media.lower)= media.upper
		.invalid-feedback
			| Por favor selecione uma mídia válida.

mixin queries
	label(for="queries") Aspecto de análise:
		select#queries.custom-select.d-block.w-100(required="", disabled="")
			option(value="") Escolha...
		.invalid-feedback
			| Por favor selecione um aspecto válido.

mixin categories
	label(for="categories") Categorias:
		select#categories.custom-select.d-block.w-100(required="", disabled="")
			option(value = "", selected="") Escolha...
			each category in categories
				option(value = category.val)= category.name
		.invalid-feedback
			| Por favor selecione um aspecto válido.

mixin actors
	#actors.form-check
		p.text-center Não há categoria selecionada